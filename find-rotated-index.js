function findRotatedIndex() {
  //binary search to find "wraparound" point
  //cut array in half and glue it back together correctly
  //binary search for num
  //return index (recutting and regluing array into original order)
}

module.exports = findRotatedIndex;
